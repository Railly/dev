---
import { LOGIT_PROJECTS } from "@/data/projects";
import ProjectCard from "./project-card.astro";
---

<section>
  <h2 class="text-lg font-semibold mb-6">Upcoming Projects</h2>
  <div class="space-y-6">
    {LOGIT_PROJECTS.map((project) => <ProjectCard project={project} />)}
  </div>
</section>
